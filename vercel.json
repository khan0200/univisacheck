{
    "version": 2,
    "builds": [{
        "src": "api/**/*.js",
        "use": "@vercel/node"
    }],
    "routes": [{
        "src": "/api/check-status/(.*)",
        "dest": "/api/check-status.js"
    }]
}